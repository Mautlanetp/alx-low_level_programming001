#include "main.h"
#include <string.h>

/**
 * main - print helberton
 *
 * Return: Always (0)
 */

int main(void)
{
	char c[] = "_putchar";
	int length, i;

	length = strlen(c);

	for (i = 0; i < length; i++)
	{
		_putchar(c[i];
				}

	_putcher('\n');

	return (0);
	}
