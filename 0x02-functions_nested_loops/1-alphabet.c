#include "main.h"

/**
 * main - prints lowercase alphabets
 *
 * Return: Always 0
 */

void print_alphabet(void)
{
	int ch;
	
	clrscr();

	for (ch= 'a'; ch<= 'z'; ch++)
		putchar(ch);
		putchar ('\n');
		getch()'
}
